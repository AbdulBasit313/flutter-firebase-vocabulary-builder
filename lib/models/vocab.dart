class Vocab {
  final String docId;
  final String uid;
  final String word;
  final String partsOfSpeech;
  // final String meaning;
  // final String sentence;

  Vocab({this.docId, this.uid, this.word, this.partsOfSpeech});
  // Vocab({this.word, this.partsOfSpeech, this.meaning, this.sentence});
}
